import React from "react"

const WorkInProgress = () => (
  <div className="main-text">
    <p style={{ marginBottom: `25px` }}>
      Uh oh! Looks like this page is still a work in progress.
    </p>

    <p style={{ marginBottom: `0` }}>
      Hi there, I appreciate you dropping by but the content is not ready.
    </p>

    <p style={{ marginBottom: `25px` }}>
      Drop me a message if you wish to be notified when this is ready.
    </p>
  </div>
)

export default WorkInProgress
